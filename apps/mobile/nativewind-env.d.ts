/// <reference types="nativewind/types" />

// This one line is what teaches TypeScript that View, Text, Pressable and the
// rest accept `className`. Without it every styled element in the app is a
// type error - roughly 280 of them.
